import { Sometype_Mono, Inter } from "next/font/google";

export const someTypeFont = Sometype_Mono({
  subsets: ["latin"],
  variable: "--font-someType",
});

export const interFont = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});
